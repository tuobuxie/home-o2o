# 详细设计文档 - 家政无忧 App

## 1. 技术栈
- **核心框架**: React 18
- **语言**: TypeScript
- **样式**: Tailwind CSS (Mobile First策略)
- **路由**: React Router (使用 HashRouter 适配无后端环境)
- **图标**: Lucide React
- **组件库**: 自研轻量级组件（避免引入重型UI库导致加载慢）

## 2. 目录结构
- `components/`: 通用UI组件 (Button, Input, Card, Modal)
- `pages/`: 页面级组件 (Home, Detail, Booking, Result)
- `types.ts`: TypeScript 类型定义
- `services/`: 模拟API服务 (Mock Data & Payment Logic)

## 3. 数据模型设计 (TypeScript Interfaces)

### Service (服务)
```typescript
interface ServiceItem {
  id: string;
  title: string;
  price: number;
  unit: string; // '小时', '次', '平米'
  image: string;
  description: string;
  tags: string[];
}
```

### Order (订单)
```typescript
interface Order {
  serviceId: string;
  date: string;
  time: string;
  address: string;
  contact: string;
  phone: string;
  totalPrice: number;
  status: 'pending' | 'paid' | 'completed';
}
```

## 4. 关键流程实现

### 4.1 支付宝支付模拟
为了演示真实的支付体验，设计了一个前端模拟的支付网关：
1. **唤起支付**：用户点击支付按钮，前端验证表单。
2. **显示网关**：弹出一个全屏/半屏 Modal，背景模糊。
3. **二维码展示**：Modal 中显示一个 Mock 的支付宝二维码（通过 API 生成或静态图），包含支付宝Logo和订单金额。
4. **状态轮询**：UI展示“正在等待用户扫码/支付”。
5. **模拟成功**：`setTimeout` 3秒后，关闭 Modal 并重定向至 `/result`。

## 5. UI/UX 细节
- **图片质量**: 使用 Unsplash API 图片，替换原有的 Lorem Picsum，确保图片内容与服务类型（清洁、家电等）强相关。
- **Sticky Footer**: 详情页和下单页的按钮必须固定在底部，方便单手操作。
- **Form Validation**: 提交订单前校验手机号和地址非空。
- **Feedback**: 各种操作给予 Toast 或 Loading 反馈。